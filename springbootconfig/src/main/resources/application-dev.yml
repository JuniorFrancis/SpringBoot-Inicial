#PROD

app:
  message: This is property file to the ${spring.application.name}

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: <USERNAME>
    password: <PASSWORD>